# Compatibility placeholder; pyproject.toml owns dependency declarations.
# This file does not install sample-export. From the repository root use:
#   python -m pip install ./sample-tools
# The runtime uses the stdlib and system-installed ffmpeg/ffprobe.
